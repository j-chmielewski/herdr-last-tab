[package]
name = "herdr-last-tab"
version = "0.1.0"
edition = "2024"
description = "Herdr plugin for toggling recently used tabs and panes"

[dependencies]
fs2 = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
